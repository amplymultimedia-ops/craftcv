{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && bun install && bun run prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && bun run src/index.ts"
  }
}
